Check main.m for estimating the model, using both MATLAB optimizers. 

For estimating the nested models, the parameters controlling the spillovers between the volatility components were set to zero. 
